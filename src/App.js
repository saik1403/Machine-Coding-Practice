import ReactDOM from 'react-dom/client';
import FormPractice from './MC 1/FormPractice';

const App = () => {
    return (
        <FormPractice />
    )
}

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />)
